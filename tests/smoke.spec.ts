import { expect,test } from "@playwright/test";
const routes=["/","/work","/music","/contact"];
for(const width of [360,430,768,1024,1440])test(`routes fit at ${width}px`,async({page})=>{await page.setViewportSize({width,height:900});for(const route of routes){await page.goto(route);await expect(page.locator("main")).toBeVisible();const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow,`${route} has horizontal overflow`).toBe(false)}});
test("all 18 work links and direct contact actions are reachable",async({page})=>{await page.goto("/work");await expect(page.locator(".button",{hasText:"Visit site"})).toHaveCount(19);await page.goto("/contact");await expect(page.getByRole("link",{name:/WhatsApp \+91/})).toBeVisible();await expect(page.getByRole("link",{name:"prathambatra68@gmail.com"})).toBeVisible()});
test("reduced motion keeps home content and stops orbit",async({page})=>{await page.emulateMedia({reducedMotion:"reduce"});await page.goto("/");await expect(page.getByRole("heading",{name:/Ideas in motion/})).toBeVisible();await page.goto("/work");await expect(page.getByRole("heading",{name:"Every site, within reach."})).toBeVisible()});
