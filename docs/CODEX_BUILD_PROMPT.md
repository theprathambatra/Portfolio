# Codex Cloud build prompt

Work on the selected `codex/portfolio-source-of-truth` branch of `theprathambatra/Portfolio`.

First read, in order, `AGENTS.md`, `WHAT_NOT.md`, `PORTFOLIO_BRIEF.md`, all files in `content/`, `docs/IMPLEMENTATION_PLAN.md`, `docs/INTEGRATIONS.md` and `docs/ACCEPTANCE_CRITERIA.md`. Inspect every supplied asset in `public/assets` and every reference page in `public/reference/byob`.

Then implement the complete production V1 as a strict TypeScript Next.js App Router application for Vercel. Do not return only a plan and do not ask me to re-upload material that already exists in this branch. Create `docs/BUILD_STATUS.md`, plan briefly there, and immediately continue into implementation.

Follow the phases in `docs/IMPLEMENTATION_PLAN.md`. Prioritise a coherent, authored result over generic component-library output. The core Home sequence must include the supplied greeting animation, the continuously traversing football character and handle, the ball-kick transition, work experience, the briefcase close, education, the postcard-to-Spotify transition and the footer. Build Work Orbit with all 18 websites and an accessible mobile fallback. Build the scalable Music Vault integration layer and the complete Contact page.

Never invent work history, dates, certifications, website results, social posts, Spotify availability or statistics. Do not reproduce the unsupported metrics in the BYOB reference. Use the exact disclosure for unverified Swawlambi company claims. Keep every secret server-side and commit only `.env.example` names.

Where third-party credentials are unavailable, build and test an honest curated fallback, then document the exact environment variable and user action needed to activate the live integration. Do not scrape Instagram or LinkedIn. Do not display a broken booking calendar.

Use GSAP ScrollTrigger for the narrative choreography, with careful cleanup, reverse-scroll stability, reduced-motion equivalents and intentionally simplified mobile scenes. Use 3D only for Work Orbit and lazy-load it. Do not violate any item in `WHAT_NOT.md`.

Before finishing, run type-check, lint, unit tests, Playwright smoke tests and the production build. Test 360, 430, 768, 1024 and 1440 CSS pixel layouts. Fix all failures you can. Update `docs/BUILD_STATUS.md` with completed work, test results and only genuine external blockers. Finish with a concise list of changed files and the exact next deployment steps.
